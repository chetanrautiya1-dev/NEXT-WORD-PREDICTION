# NEXT-WORD-PREDICTION
Next Word Prediction using LSTM is a Natural Language Processing and Deep Learning project that predicts the most likely next word based on previously entered words. The model learns word patterns and context from a text dataset and uses an LSTM (Long Short-Term Memory) network to generate next-word predictions.
